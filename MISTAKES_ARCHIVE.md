# MISTAKES & LEARNINGS — COLD ARCHIVE
# Style: Ultra-terse, zero fluff, token-efficient. Cold archive for targeted grep retrieval during debugging & distillation.
# Entry Schema: [DATE] ID | BAD: <symptom/error> | WHY: <root cause> | FIX: <fix applied> | RULE: <future prevention>

## Domain: Web Portal & Frontend UI
- [2026-08-20] WEB_PORTAL_UX | BAD: Equipment paperdoll hover tooltip clears when moving mouse towards it. | WHY: onMouseLeave was bound to individual slots, clearing state when crossing grid gaps. | FIX: Changed onMouseEnter to onClick to pin the state persistently. | RULE: For tooltips in tight grids, use pinned click states instead of fragile hover regions.
- [2026-08-20] WEB_PORTAL_DATA | BAD: Monster and item names display as "Item #1234". | WHY: Server lacked native dictionary and SQL joins are over-engineered. | FIX: Wrote build-time TS script to extract names from db/re YAMLs into a static TS dictionary. | RULE: Use static build-time dictionaries generated from game files instead of runtime DB joins when possible.
- [2026-08-22] REACT_PORTING | BAD: Left legacy RO classes in Header, LoginModal, and PublicSearch | WHY: Forgot to migrate some components when replacing the CSS framework | FIX: Replaced old classes with new Bento tokens | RULE: When rewriting a global CSS file, use grep to find and migrate all usages of deleted legacy classes across the entire codebase.
- [2026-08-22] YAGNI_WEB_WRITE | BAD: Added direct primary DB writes from web portal to trigger dispatch timer | WHY: Blindly implemented user action as a write without realizing game engine tracks logout time passively | FIX: Switched to automatic offline rest accrual on logout; reverted web portal to 100% read-only on replica | RULE: Before adding write endpoints to a read-only architecture, check if state can be derived passively from existing game engine timestamps.
- [2026-08-22] WEB_DB_WRITE_POOL | BAD: INSERT/REPLACE failed with 'INSERT, DELETE command denied to user ro_user' | WHY: Used default read pool (ro_user on replica port 3307) for dispatch write mutation | FIX: Use primaryExecute from pool.ts which connects to primary DB (port 3306) with write credentials | RULE: All database mutations (INSERT/UPDATE/DELETE/REPLACE) in the web portal backend MUST use primaryExecute or primaryQuery, never the read-only replica pool
- [2026-08-23] REACT_ROSTER_OPTIMISTIC_SYNC | BAD: Hero tab offline rest and yield did not update when switching roster | WHY: StatusWindow waited on async selectedCharDetail, lacked component key, and useEffect omitted char.charId | FIX: Optimistically derive activeChar from in-memory roster, add key={activeChar.charId}, and include char.charId in effect deps | RULE: For master-detail selection, derive active entity from in-memory list during async load, key subcomponents by ID, and include ID in effect deps.
- [2026-08-23] SQL_COALESCE_DYNAMIC_TIMESTAMP | BAD: Offline rest reported 0s elapsed time on every query for unplayed characters | WHY: COALESCE fallback evaluated UNIX_TIMESTAMP() to NOW() at query execution time | FIX: Default COALESCE fallback to 0 instead of UNIX_TIMESTAMP() | RULE: Never use dynamic functions (UNIX_TIMESTAMP, NOW) as COALESCE fallback defaults for historical timestamp columns.
- [2026-08-23] CHART_SYNTHETIC_CLIFF | BAD: candlestick chart showed artificial vertical cliff drop and fake history spanning back 24h | WHY: bootstrap seeder generated a random walk that drifted from the actual DB price before the first real shift ran | FIX: removed synthetic seeder completely, truncated history tables to allow clean organic live tracking, and configured integer price scale formatting | RULE: never inject unanchored fake time-series data into live tables; always let production charts build organically from live cron shifts.
- [2026-08-23] DEMO_FILE_LOCATION | BAD: placing interactive prototypes/demos in web root | WHY: cluttered repository root instead of following workspace documentation | FIX: place all HTML wireframes and interactive demos inside `web/demos/` (and historical in `web/demos/archive/`) | RULE: NEVER place demo HTML files in `web/` root; always put them in `web/demos/` and link from `web/demos/index.html`.
- [2026-08-23] UI_MODAL_ERGONOMICS | BAD: AdminVaultWindow used legacy RoWindow with #141b28, font-cinzel, dead minimize button, no Esc key listener, no backdrop click | WHY: UI drift from Bento design system | FIX: refactored to Bento modal dialog with semantic tokens, Esc listener, backdrop dismiss, and security drawer | RULE: enforce semantic design tokens (bg-surface, bg-surface2, border-border, text-accent) and standard modal ergonomics (Escape, backdrop dismiss, min-h 40px touch targets) across all web components
- [2026-08-23] UI_DOMAIN_METADATA | BAD: Conflating domain gameplay IDs (Item/Mob/Map) with plumbing leaks, while leaking accountId and replica ports (:3307) | WHY: Lack of clear classification taxonomy between player-facing gameplay metadata (wiki lookups, farming routes) and backend infrastructure plumbing | FIX: Preserved Item IDs, Mob IDs, Card IDs, and Map Asset names; scrubbed accountId (#2000001) from user badges and replaced :3307/MariaDB Replica with in-universe live connection copy | RULE: In web game portals, always preserve domain gameplay identifiers for community cross-referencing; strictly scrub authentication IDs, database ports, and server cluster roles.
- [2026-08-24] UI_MODAL_SCOPE_REGRESSION | BAD: Blank black page when clicking ticker row in MarketWatch | WHY: Partial edit in `TickerDetailModal.tsx` stripped `profile`, `activeEvents`, and `historicalEvents` derived variable declarations, throwing runtime ReferenceError on render | FIX: Restored derived variable declarations (`profile = MUNICIPAL_LORE[quote.ticker]`, `activeEvents`, `historicalEvents`, `hasLiveOrHistoricalNews`) | RULE: Always check component-level scope variable declarations when replacing JSX fragments in React components.
- [2026-08-25] REACT_UNDECLARED_PROP_REF | BAD: ReferenceError: refreshData is not defined when opening Gacha tab | WHY: App.tsx passed undeclared `refreshData` prop instead of declared `loadUserData` callback | FIX: Updated `onRefreshBalances={loadUserData}` in `App.tsx` | RULE: Always cross-reference exact function/callback names in parent scope before wiring child component props.
- [2026-08-25] ADMIN_ROLE_GUARD | BAD: standard player (`groupId: 0`) seeing admin management button | WHY: fallback to auto-seeded `localStorage` admin key in client | FIX: strictly require `user && user.groupId >= 1`; removed localStorage auto-seeding | RULE: never use client localStorage fallback to determine user role permissions
- [2026-08-25] HARDCODED_EXTERNAL_CONFIG | BAD: Hardcoded `PLAYLIST_ID = "PLLlAbcezVXFM"` directly inside `AudioContext.tsx`. | WHY: Violated Zero-Trust on Hardcoded Literals by embedding external third-party YouTube playlist ID into application source code. | FIX: Parameterized via `import.meta.env.VITE_YOUTUBE_PLAYLIST_ID` with fallback. | RULE: External media playlists, webhook URLs, and third-party resource IDs must always be injected via environment variables and configuration schemas.
- [2026-08-25] MOBILE_AUDIO_DOCK_COLLISION | BAD: Floating GlobalAudioDock hovered directly over fixed mobile bottom navigation tabs (Hero/Finance) | WHY: GlobalAudioDock used fixed bottom-4 right-4 z-40 without accounting for mobile bottom-0 navigation bar height or responsive control density | FIX: Elevated dock position to bottom-[calc(3.75rem+env(safe-area-inset-bottom,0px))] left-3 right-3 sm:left-auto md:bottom-4 md:right-4, hid secondary controls on mobile, and increased main container bottom padding | RULE: Any persistent floating overlay must dynamically account for mobile bottom navigation bars and safe area insets via responsive bottom offsets and content clearance padding.
- [2026-08-25] MARKET_SIM_TRUNCATION_BIAS | BAD: Stock tickers persistently fall and rarely uptrend in market simulation. | WHY: `Math.floor((price * f) / 100)` truncated fractional gains to 0 on sub-100z tickers while negative drops rounded down to -1, starving `upCount` and trapping the market in permanent Bearish mood (`f -= 2`). | FIX: Implemented symmetric `Math.round`, guaranteed minimum 1-unit tick on non-zero percentage shifts, and balanced mood transition based on `upCount > downCount`. | RULE: Use symmetric rounding and minimum tick guarantee for integer-scale pricing simulations to prevent asymmetric downward drift traps.
- [2026-08-25] AUDIO_IFRAME_PORTAL_SINGLETON | BAD: Background audio reset to 0:00 when navigating back to Leisure tab. | WHY: Separate iframes in LeisureView and GlobalAudioDock unmounted/remounted each other on tab switch. | FIX: Single singleton iframe in GlobalAudioDock portaled via createPortal into #leisureVideoSlot. | RULE: Never mount duplicate media iframes across views; use single persistent root portal for seamless background audio.
- [2026-08-25] REACT_DEFENSIVE_PROPS | BAD: TypeError: Cannot read properties of undefined (reading 'find') in BankWidget. | WHY: Array prop characters accessed with .find without default fallback []. | FIX: Default characters = [] + safeChars array fallback + optional chaining. | RULE: Always provide default empty arrays and safe fallbacks for collection props in React components.
- [2026-08-26] JSX_HTML_COMMENT_PARSING | BAD: Vite build failure with 'Expected identifier but found "!"' in JSX | WHY: accidental HTML comments (<!-- -->) instead of JSX ({/* */}) in React component | FIX: replaced with JSX comments | RULE: never write raw HTML comments inside React JSX markup

## Domain: Monorepo, Build & Container Configuration
- [2026-08-16] BUN_WIN_EPERM | BAD: `bun install` fails with `EPERM: Operation not permitted (NtSetInformationFile())` on Windows cache | WHY: Bun hardlink cache collision on NTFS | FIX: use `bun install --backend=copyfile --no-cache` | RULE: On Windows PowerShell, use `--backend=copyfile --no-cache` if Bun hardlink cache fails
- [2026-08-16] BUN_DOCKER_MONOREPO | BAD: Docker build fails with `/bin/sh: vite: not found` or unresolved modules | WHY: workspace hoisting in Alpine Linux + devDependencies missing in subpackages | FIX: copy full workspace trees before install, keep build tools in dependencies, run `bun build` directly | RULE: reference `bun-monorepo-containerize` skill for multi-stage Bun + Vite Docker recipes
- [2026-08-16] VITE_DOCKER_TYPES | BAD: `error TS2688: Cannot find type definition file for 'vite/client'` during container build | WHY: explicit `types: ["vite/client"]` in tsconfig fails when type roots are hoisted | FIX: use `src/vite-env.d.ts` with `/// <reference types="vite/client" />` and `"moduleResolution": "bundler"` | RULE: use `vite-env.d.ts` instead of `types` array in monorepo tsconfigs
- [2026-08-22] DOCKER_PRIMARY_DB_ENV | BAD: connect ECONNREFUSED 127.0.0.1:3306 on web write queries | WHY: web-portal in docker-compose.yml lacked PRIMARY_DB_HOST env var, defaulting to loopback 127.0.0.1 inside container | FIX: Added PRIMARY_DB_HOST: db to web-portal service in docker-compose.yml | RULE: When introducing a new DB connection pool in web services, always configure its hostname/port in docker-compose.yml and verify with an integration test inside the container.
- [2026-08-22] DOCKER_BUILD_WITHOUT_UP | BAD: User did not see web portal updates after build | WHY: 'docker compose build' only builds the image; it does not recreate or restart the running container | FIX: Run 'docker compose up -d --no-deps web-portal' after building to recreate the running container with the new image | RULE: Always run 'docker compose up -d --build --no-deps <service>' or follow 'docker compose build' with 'docker compose up -d' when updating running web containers.
- [2026-08-23] BUN_WORKSPACE_UNLINKED | BAD: Cannot find module '@rathena/shared' during bun test | WHY: workspace packages not linked after branch switch | FIX: run bun install at web/ monorepo root | RULE: always run bun install at web/ root before running monorepo tests
- [2026-08-23] DOCKER_COMPOSE_WEB_REBUILD | BAD: Web changes not active in live container | WHY: Forgot to trigger docker compose rebuild after web frontend/backend modifications | FIX: Run `docker compose -f tools/docker/docker-compose.yml up -d --build web-portal` | RULE: Always rebuild docker compose immediately after web portal changes
- [2026-08-23] DOCKERIGNORE_NESTED_NODE_MODULES | BAD: `error: ENOENT reading "/app/apps/server/node_modules/elysia"` in Linux container | WHY: `.dockerignore` had root `node_modules` instead of `**/node_modules`, leaking host Windows NTFS symlinks (`E:/...`) into Alpine Linux container | FIX: Update `.dockerignore` to `**/node_modules` and `**/dist` | RULE: Always use `**/node_modules` and `**/dist` in monorepo `.dockerignore`

## Domain: Edge Case NPC Scripting & Logic Migrations
- [2026-08-02] DOEVENT_NO_PLAYER_ATTACH | BAD: `buildin_doevent: fatal error ! player not attached!` | WHY: `doevent` called in `OnInit` / detached context requiring player RID | FIX: use `donpcevent` instead of `doevent` | RULE: use `donpcevent` for server/system/OnInit triggers; use `doevent` only when caller has active player attached
- [2026-08-02] SCRIPT_INFINITY_LOOP_FREELOOP | BAD: `script:run_script_main: infinity loop !` on server boot / large loop | WHY: rAthena `check_gotocount` (2048) exceeded by multi-day catch-up or large dataset loops | FIX: wrap heavy loops / batch subroutines with `freeloop(1);` and `freeloop(0);` | RULE: always wrap catch-up loops, batch player processing, or table iterations in `freeloop(1)` / `freeloop(0)`
- [2026-08-02] SCRIPT_FALLTHROUGH_END | BAD: unintended execution cascade into next event handler | WHY: missing `end;` after `OnClock0000:` fell through into `OnClock0300:` | FIX: add `end;` at end of event handler block | RULE: every event / timer / clock label block must terminate explicitly with `end;` or `close;`
- [2026-08-16] DYNAMIC_POOL_HARDCODING | BAD: hardcoded truncated 8-item array in script despite generator tool | WHY: rAthena script lacks runtime YAML parser; artificially limited variety | FIX: harvest ALL valid drops into MySQL table `custom_junk_pool` and query dynamically via `query_sql` + `ORDER BY RAND()` | RULE: never copy/hardcode generated data pools into NPC scripts; store in SQL table and load dynamically with `query_sql`
- [2026-08-22] NPC_SELECT_MENU | BAD: select() menu choices splitting into extra lines and breaking index mapping | WHY: Used colon (:) inside a menu option string, which rAthena select() uses as a delimiter | FIX: Replaced inner colons with hyphens (-) | RULE: Never use colons (:) inside the display text of select() options.
- [2026-08-22] DEAD_CALLSUB_ONINIT | BAD: 'buildin_callsub: Argument is not a label' and 'player not attached' on server boot | WHY: Market simulation & event seeding moved to Elysia/SQL, but dead catch-up loop in StockExchangeSetup::OnInit still called deleted subroutines (L_MidnightProc, L_SeedEventsDef) | FIX: Removed legacy catch-up loop and dead callsubs, leaving clean DB default initialization on boot | RULE: When migrating NPC logic to background microservices/SQL, remove obsolete in-script subroutines and their callers in OnInit.
- [2026-08-16] SQL_RESERVED_WORDS | BAD: query parse error on `int`, `str`, `class` columns in `char` table | WHY: unescaped SQL reserved keywords in MariaDB | FIX: wrap all column and table names in backticks (e.g. `` `int` ``, `` `class` ``) | RULE: always wrap rAthena table and column names in backticks in SQL query strings
- [2026-08-20] CONTEXT_GROUNDING | BAD: spit out generic fintech/stock portfolio design ignoring workspace domain | WHY: assumed generic web domain without checking repo context (rathena-solo-centric / custom scripts / web visualizer) | FIX: logged mistake, anchored to repository scope | RULE: always inspect codebase context and clarify project-specific domain before generating solutions for broad prompts
- [2026-08-23] NPC_DIALOGUE_OVERFLOW | BAD: dynamic tickers/items below top 5-6 invisible in client | WHY: RO dialog box fits only 6-8 lines; unpaginated mes loop overflowed client window buffer | FIX: paginated loop with 4-5 items per page, [Page X/Y] header, and next; | RULE: always paginate dynamic SQL/array output to 4-5 lines per dialog page with next;
- [2026-08-23] FEATURE_PHASE_SCOPE | BAD: enabled unrequested future Phase 2 expansion | WHY: assumed rollout intent instead of strict phase confinement | FIX: reverted Phase 2 to enabled = 0 | RULE: keep unrequested expansion phases strictly disabled unless user explicitly requests them
- [2026-08-23] NPC_DEAD_CALL_PRUNING | BAD: 'buildin_callfunc: Function not found! [F_MoodNameStr]' in Market Pulse App | WHY: Attempted to resurrect and implement an orphaned legacy function instead of deleting dead code | FIX: Removed obsolete MarketMood query and callfunc from system_tablet.txt; stock quotes and headlines render cleanly | RULE: When an unmaintained callfunc fails in an NPC script, prune the obsolete call rather than bloating shared function libraries with unnecessary code.
- [2026-08-23] HARDCODED_ID_SETS | BAD: Fragile hardcoded MVP_MOB_IDS / MINI_BOSS_IDS sets in TypeScript services | WHY: Build-time name extractor only pulled names, discarding MvpExp and Mode.Boss YAML flags | FIX: Enhanced extract-names.ts to parse MobTypes directly from mob_db.yml; deleted hardcoded numeric sets | RULE: Always generate domain classifications and metadata from game YAML databases at build time instead of hardcoding numeric ID sets.
- [2026-08-25] DIVIDEND_ZERO_STUCK | BAD: All active enabled stock dividends stuck at 0z in solo_stock_market | WHY: marketMood === 3 (Chaos) wiped dividend to 0 in master table, 10:1 stock split integer divided dividend / 10 to 0, and slow 30% +/-1z velocity prevented recovery before next wipe | FIX: Replaced destructive zero-wipe with dynamic target yield tracking, recalibrated post-split dividend to target yield of new price, and restored live DB dividends | RULE: Never zero out master stock table dividends on market mood; recalibrate yield dynamically on stock splits.
- [2026-08-25] NPC_NUMERIC_INPUT_LIMIT | BAD: Player inputs 100m (100,000,000) into NPC input dialog but server reads 10m (10,000,000) | WHY: Ragnarok Online client numeric edit dialog (ZC_OPEN_EDITDLG / input .@var;) has hardcoded 8-character max length in client UI, discarding 9th digit of 100,000,000 | FIX: Use string input dialog (ZC_OPEN_EDITDLGSTR / input .@str$;) and parse via F_ParseZenyInput supporting shorthand ('100m', '50k', '1b') and large numeric strings | RULE: For any NPC script accepting Zeny >= 100M, always use input .@str$; with F_ParseZenyInput instead of numeric input .@num;
- [2026-08-25] ASSET_CLASS_SECTOR_FILTER | BAD: Disabling active Phase 13 crypto tokens to pass CI destroyed live holder positions | WHY: CI test and scripts conflated asset classes by filtering with fragile `LIKE '%Protocol%'` instead of explicit ticker sets, causing 8 crypto tokens (YMI, WRP, SHD, ZEX, ORA, POR, NZN, KFX) to leak into municipal equities | FIX: Restored Phase 13 tokens (enabled = 1), updated NPC scripts and CI test suite to partition asset classes via explicit `ticker IN / NOT IN ('EMP','YMI','WRP','SHD','ZEX','ORA','POR','NZN','ALM','KFX')` | RULE: Never disable live asset classes to silence CI test failures; update test harness and script queries to match multi-asset domain partitions.
- [2026-08-26] REACT_CATEGORY_MOOD_SYNC | BAD: MarketWatch header showed 'Crypto: Neutral' despite 10 bearish crypto tickers | WHY: App.tsx omitted cryptoMood from public state & never passed cryptoMood/equitiesMood props; MarketWatch fallback defaulted to 0 | FIX: Added cryptoMood state in App.tsx, passed full mood props to MarketWatch, and added dynamic quote fallback in MarketWatch | RULE: Wire all domain sentiment/meta properties down through parent state and compute defensive consensus fallbacks from live collections.
- [2026-08-26] REF_ERROR_REFACTOR_LEAK | BAD: HTTP 500 on /api/economy/net-worth and ReferenceError: CRYPTO_TICKERS is not defined in client bundle | WHY: Refactoring duplicate ticker sets left dead `isCrypto()` call in `economy.service.ts` and trailing `CRYPTO_TICKERS.has()` in `StockPortfolio.tsx` | FIX: Replaced all occurrences with `isCryptoAsset()` from `@rathena/shared` and added automated `getNetWorthSummary` unit test to CI | RULE: Always grep the entire codebase for deleted identifier names across both frontend and backend before finalizing refactors.
- [2026-08-27] SQL_UPDATE_EVAL_ORDER | BAD: cost basis distorted on partial share sales (e.g. 1200z became 667z) | WHY: In MySQL `UPDATE ... SET shares = shares - qty, total_cost = total_cost - ROUND((total_cost * qty) / shares)`, `SET` evaluates left-to-right, dividing by already mutated `shares` | FIX: Use known pre-sale variable `.@s` in script `total_cost = total_cost - ROUND((total_cost * qty) / .@s)` before modifying `shares` | RULE: In SQL UPDATE SET clauses with dependent calculations, compute dependent expressions first or use pre-calculated parameters rather than mutating the column in-place.






