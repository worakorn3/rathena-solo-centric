description = "Slash command to generate new Gemini components (commands, skills, agents) using the Component Creator agent"
prompt = "Please research, draft, and generate a new Gemini component (command, skill, or agent) using the specialized creator sub-agent based on: {{args}}"
