# MISTAKES & ACTIVE ACTION ITEMS (HOT TIER)
# Style: Ultra-terse, high-density, token-efficient.
# Cold archive stored in MISTAKES_ARCHIVE.md.
<!-- HARD CEILING: Max 10 domain rows (< 350 tokens). Merge or retire when adding new domains. -->

## Aggregated Invariants & Reinforced Guardrails Matrix
| Frequency | Aggregate Domain | Core Invariants & Sub-Rules | Executable Prevention Guardrail / Action Item |
| :--- | :--- | :--- | :--- |
| **CRITICAL (23x)** | `NPC_ENGINE_SAFETY` | Syntax, scoping, inventory, state, pagination & encoding | Literal `\t` headers; prefix all temp vars with `.@`; `if (!checkweight(id, amt)) close;`; validate before `#var` writes; paginate 4-5 lines with `next;`; zero UTF-8 emojis (ASCII only); wrap batch loops in `freeloop(1/0)` |
| **CRITICAL (16x)** | `CONTAINER_TOOLING` | Docker testing, Windows tooling, compose & monorepos | Test map-server via Docker on port 5122 (`conf/map_test.conf`); native AI file tools only (NO `>>`/`echo` on Windows); run `docker compose up -d --build --no-deps <svc>`; use `**/node_modules` in `.dockerignore` |
| **HIGH (12x)** | `DB_ARCHITECTURE` | Split replica/primary, custom migrations & SQL syntax | Reads on replica (:3307), mutations via `primaryExecute` (:3306); base rAthena SQL immutable (`sql-files/custom/` only); wrap identifiers in backticks (``` `col` ```); no dynamic functions in `COALESCE` defaults |
| **HIGH (8x)** | `DESIGN_SYSTEM_UX` | Bento design tokens, modal ergonomics & privacy | Standardize on Bento tokens (`bg-surface`, `text-accent`); Esc & backdrop dismiss; min 40px touch targets; scrub infra plumbing (`:3307`, `accountId`); offset mobile overlays with `env(safe-area-inset-bottom)` |
| **HIGH (8x)** | `REACT_STATE_DEFENSE` | Component scope, defensive props & fault isolation | Inspect full parent scope/derived vars before chunk edits; fallback `items = []` and optional chaining `?.`; derive active entity in-memory + key by ID; wire full domain sentiment props down parent tree; wrap dynamic modals/charts in `<ErrorBoundary>` |
| **MEDIUM (7x)** | `BUILD_EXTRACTION` | Zero hardcoded literals, YAML generation & auth | Parameterize external IDs via `import.meta.env`; extract YAML metadata (`mob_db.yml`) to static TS at build-time (no hardcoded numeric ID sets); strict `user.groupId >= 1` (zero localStorage auth trust) |
| **MEDIUM (6x)** | `MATH_ECONOMICS` | Simulation rounding, yield tracking & time-series | Symmetric `Math.round` + guaranteed min 1-unit tick for integer pricing; dynamic target yield tracking (no zero-wipes); organic live shift recording (no unanchored fake history); integer price formatting |
| **MEDIUM (5x)** | `WORKFLOW_DISCIPLINE` | Planning preservation, domain grounding & phase scope | Plan first & continuously append to `implementation_plan.md`; test locally before finishing; ground designs in RO game domain; keep unrequested expansion phases strictly disabled (`enabled = 0`) |

## Hot Aggregated Caveman Log
- [2026-08-25] NPC_ENGINE_SAFETY [FREQ: 23] | BAD: syntax crash, var leaks, item loss, infinite dupes, overflow text, mojibake | WHY: space headers, bare vars, missing checkweight, unpaginated mes, UTF-8 emojis | FIX: `\t` headers, `.@` vars, `checkweight`, `next;` pagination, pure ASCII, `freeloop` | RULE: lint scripts with `lint_npc_dialogue.py` and run Docker map-server `--run-once`
- [2026-08-25] CONTAINER_TOOLING [FREQ: 16] | BAD: port collisions, UTF-16 corruptions, stale containers, Alpine ENOENT | WHY: host map-server runs, shell `>>`, missing `--build`, Windows symlinks | FIX: port 5122 test config, AI file tools, `compose up -d --build`, `**/node_modules` | RULE: always test in Docker isolation and use native AI file manipulation tools
- [2026-08-25] DB_ARCHITECTURE [FREQ: 12] | BAD: read-only permission denied, schema drift, syntax errors on reserved keywords | WHY: writes on port 3307, altering base SQL, unquoted `class`/`int` | FIX: `primaryExecute` on 3306, custom migrations only, backtick column wrapping | RULE: isolate mutations to primary pool and preserve base rAthena SQL immutability
- [2026-08-25] DESIGN_SYSTEM_UX [FREQ: 8] | BAD: skeuomorphic drift, trapped modals, infra port leaks, bottom dock collisions | WHY: hardcoded hex colors, missing Esc listeners, leaking :3307, fixed bottom-4 on mobile | FIX: Bento semantic tokens, Esc/backdrop dismiss, scrub infra IDs, safe-area-inset offset | RULE: enforce Bento token standard, privacy scrubbing, and mobile viewport ergonomics
- [2026-08-25] REACT_STATE_DEFENSE [FREQ: 7] | BAD: blank screens, ReferenceError on derived vars, Cannot read properties of undefined | WHY: chunk edits stripping variable declarations, missing default `[]` array props | FIX: inspect full component scope before edit, provide safe fallbacks, wrap in `<ErrorBoundary>` | RULE: verify scope variables before replacing JSX fragments and provide defensive defaults
- [2026-08-25] BUILD_EXTRACTION [FREQ: 7] | BAD: hardcoded YouTube IDs, fragile MVP ID arrays, unauthorized admin buttons | WHY: embedding 3rd-party IDs, skipping YAML parser, client localStorage auth fallback | FIX: `import.meta.env`, build-time YAML extraction to TS, server `groupId >= 1` check | RULE: zero-trust on hardcoded literals; extract domain data from game YAMLs at build time
- [2026-08-25] MATH_ECONOMICS [FREQ: 6] | BAD: downward stock drift trap, 0z stuck dividends, artificial chart cliff | WHY: Math.floor fractional truncation, mood zero-wipes, unanchored synthetic seeder | FIX: Math.round + 1-unit min tick, target yield recalibration, organic live shifts only | RULE: use symmetric rounding for integer pricing and never zero out master dividend yields
- [2026-08-25] WORKFLOW_DISCIPLINE [FREQ: 5] | BAD: unsolicited blind edits, lost plan context, out-of-domain designs, phase creep | WHY: jumping to code without plan, overwriting plan files, enabling unrequested phases | FIX: append-only implementation plans, test before complete, ground in RO domain, phase flags | RULE: plan and confirm before execution; keep plans as living append-only documents

*For historical 1-off debug logs and domain edge cases, see [MISTAKES_ARCHIVE.md](file:///E:/Games/Ragnarok/rathena-solo-centric/MISTAKES_ARCHIVE.md).*